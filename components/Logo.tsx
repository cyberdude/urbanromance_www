import React from "react";
import styled from "styled-components";

export const Logo = () => <LogoImg src="/URBANROMANCE-LOGO.png" />;

const LogoImg = styled.img({
  width: 260,
  marginTop: 20,
  marginBottom: 30
});
